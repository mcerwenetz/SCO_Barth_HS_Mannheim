#inlude "lib.h"

double heron(double x, double eps){
	double intinital = (1 + x) / 2;
	
	do {
		
		
		}(while (curr-prev) > eps); 
	
}

while heron_rec(double ai, double x, double eps){
	return (ai + (x/ai))/2;
}
